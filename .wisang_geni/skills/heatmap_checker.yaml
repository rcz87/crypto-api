name: heatmap_checker
description: Check dan verify heatmap endpoints
commands:
  - curl -s http://localhost:5000/api/liquidations/BTC/heatmap
  - pm2 logs crypto-api --lines 10 | grep heatmap
created_at: "2025-10-22T12:00:00Z"
