# NPM Configuration for Memory-Efficient Builds
# Increase Node.js heap size for build processes

# Set NODE_OPTIONS for all npm scripts
node-options=--max-old-space-size=512

# Alternative environment variable approach
# This ensures build tools have enough memory
max_old_space_size=512

# Reduce package-lock resolution for faster builds
prefer-offline=true
audit=false

# Optimize build performance
legacy-peer-deps=false
